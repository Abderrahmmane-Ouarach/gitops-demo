apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops-demo-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Abderrahmmane-Ouarach/gitops-demo.git
    targetRevision: main
    path: helm
    helm:
      valueFiles:
        - values.yaml
        - values.production.yaml
      parameters:
        - name: env
          value: production
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true